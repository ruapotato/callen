# Callen — Python dependencies
#
# pjsua2 is NOT in this list — it must be compiled from pjproject source.
# See README.md for build instructions.

# ASR — Parakeet speech-to-text
nemo_toolkit[asr]>=2.2.1
torch>=2.1.0
soundfile>=0.13.1
webrtcvad-wheels>=2.0.14

# TTS — Kokoro neural TTS (kept loaded for instant synthesis)
kokoro>=0.9.4

# Audio processing
numpy<2.0
resampy>=0.4.3

# Email attachment extraction (OCR for images, text for PDFs)
# Also requires the `tesseract-ocr` system package (see README).
pytesseract>=0.3.13
pdfplumber>=0.11.0
Pillow>=11.0.0

# Web interface
quart>=0.19.0
