# Callen — Python dependencies
#
# pjsua2 is NOT in this list — it must be compiled from pjproject source.
# See README.md for build instructions.

# ASR — Parakeet speech-to-text
nemo_toolkit[asr]>=2.2.1
torch>=2.1.0
soundfile>=0.13.1
webrtcvad-wheels>=2.0.14

# Audio processing
numpy<2.0
resampy>=0.4.3

# Web interface
quart>=0.19.0
