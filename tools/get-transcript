#!/bin/bash
# Get a transcript for an incident or specific call
# Usage: ./tools/get-transcript --incident INC-0001 --text
#        ./tools/get-transcript --call <uuid>
exec python3 -m callen.cli get-transcript "$@"
